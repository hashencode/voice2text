# TODO

## 语音识别能力接入（业务待实现）

- [ ] 接入说话人分离业务链路（P1）  
      现状：底层识别层已保留 `enableSpeakerDiarization` 能力，但设置页和全局开关已移除。  
      目标：在真实业务流程中定义触发时机、结果消费方式（UI 展示/数据落库/导出格式）及错误处理策略。

- [ ] 接入降噪业务链路（P1）  
      现状：底层识别层已保留 `enableDenoise` 能力，但设置页和全局开关已移除。  
      目标：在录音或转写业务中明确启用条件、性能预算（耗时/电量）、以及与不同模型组合时的验证策略。

## Sherpa FFmpeg 迁移后续

- [ ] 迁移 `react-native-sherpa-onnx` 的自动下载 + 版本校验机制（类似 `prebuilt-download.gradle`）到 `modules/sherpa/android`。
- [ ] 新增 FFmpeg 预编译资源版本清单（tag、URL、sha256、ABI 文件列表）并在构建前做完整性校验。
- [ ] 增加 Gradle 任务：本地缺失时自动下载并解压到 `modules/sherpa/android/src/main/{cpp/include/ffmpeg,jniLibs}`，存在则跳过。
- [ ] 增加“版本戳/锁文件”策略，避免团队成员因为本地缓存版本不一致导致编译或运行差异。
- [ ] 在 CI 增加校验步骤：校验预编译资源版本与 checksum，失败时直接阻断构建。
- [ ] 补充迁移文档：下载来源、升级流程、回滚流程、常见坑（符号冲突 / ABI 缺失 / tag 变更）。

## 首页录音分组重构后续

- [ ] 分组置顶能力（P2）  
      现状：分组页将承载“全部录音 + 系统分组 + 自定义分组”，当前按默认顺序展示。  
      目标：支持固定常用分组到顶部（置顶/取消置顶），降低分组变多后的查找成本。

- [ ] 在分组编辑页展示分组录音数量（P2）  
      现状：文件列表页 tabs 不展示数量；用户明确希望数量信息只出现在分组管理上下文。  
      目标：在分组编辑/管理页展示各分组数量，并与录音变更保持联动刷新。
